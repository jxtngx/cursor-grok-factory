/** Harbor / LangGraph entry. Engineers replace after spec approval. */

export function makeGraph(_config?: unknown): never {
  throw new Error(
    "cursor-grok-factory: implement after TRACK.md is typescript-langchain and the spec is approved"
  );
}
