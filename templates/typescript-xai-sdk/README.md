# Track: typescript-xai-sdk

If `cursor.pairing: together`: xAI first-party / REST **and** `@cursor/sdk`.
If xAI documents TS only via `@ai-sdk/xai`, init should have confirmed this track vs `typescript-vercel-ai`.
Model `grok-4.6` on xAI; Grok catalog id on Cursor. LangSmith tracing still on.
`grok-only` / `cursor-only`: only that client.

