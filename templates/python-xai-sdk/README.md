# Track: python-xai-sdk

If `cursor.pairing: together`:

- `from xai_sdk import Client` — `client.chat.create(model="grok-4.6")`
- `cursor-sdk` — local/cloud agent, Grok catalog pick
- LangSmith tracing around both

`grok-only` / `cursor-only`: only that client.
