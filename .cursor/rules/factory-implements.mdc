---
description: This is a factory. After the spec is approved, engineers implement tickets.
globs:
  - "**/*"
alwaysApply: true
---

# Factory, not a lab

- Before a technical-requirements file exists: only `@init-grok` / discovery. No clients.
- After the spec is approved: implement the ticket. Do not send the user to write the SDK themselves.
- One TRACK.md. Do not scaffold a second language or a second **inference** SDK.
- Cursor pairing is whatever init locked. Do not add or drop Cursor on your own.
- Grok is the default **model**. Do not silently switch to OpenAI/Anthropic.
- LangSmith tracing stays on.
