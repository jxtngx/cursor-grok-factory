# Knowledge

Used only when `harness/knobs.yaml` `knowledge.kind` is `local`.
Put retrieval corpora here after spec approval. Do not commit secrets.

`none` = session only. `aws` = S3, nothing in this folder.
